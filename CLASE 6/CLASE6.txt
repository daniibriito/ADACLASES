TEMAS:

Navegar por terminal / git bash:
cd.
ls.
Git.

Que es GIT y como funciona:
git init.
git add.
git commit.
git status.
git log.

TAREA: 

Hacer un repositorio nuevo en cada tarea que hicimos hasta ahora. En cada carpeta ingresar y escribir git init para iniciar el repo, git add seleccionando por archivo o todos los archivos al mismo tiempo, depende como deseemos hacerlo, y por ultimo git commit -m "algun mensaje"